export const APP_ID = 'journal'
export const TOKEN_LABEL = APP_ID
export const TOKEN_STORAGE_PREFIX = `oc-${APP_ID}-token:`
export const SUBSCRIPTIONS_STORAGE_PREFIX = `${APP_ID}_subscriptions_`
export const SUBSCRIPTIONS_WEBDAV_DIR = `.${APP_ID}`
export const PRODID = `-//opencloud-${APP_ID}//EN`
